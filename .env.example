# Database — SQLite by default, or PostgreSQL/Supabase URL
DATABASE_URL="file:./dev.db"

# Security Secrets — ALWAYS change to a random 64-character secret in production!
JWT_SECRET="replace-with-a-cryptographically-secure-random-string-min-32-chars"

# Optional Initial Admin Seed Password (default is 'admin123' if omitted)
ADMIN_INITIAL_PASSWORD="your-strong-admin-password-here"

# Business Contact Defaults
BUSINESS_PHONE="+91 90350 85031"
BUSINESS_WHATSAPP="+919035085031"
BUSINESS_EMAIL="support@stardigital.in"
